apiVersion: apps/v1
kind: Deployment

metadata:
  name: tetris

spec:
  replicas: 1

  selector:
    matchLabels:
      app: tetris
      version: v1

  template:
    metadata:
      labels:
        app: tetris
        version: v1

    spec:

      containers:
      - name: tetris

        image: agdevopsacr2026.azurecr.io/tetris:bc90f63a4e01b2e351611a055d6a63f187c930a2

        imagePullPolicy: Always

        ports:
        - containerPort: 80

        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"

          limits:
            cpu: "500m"
            memory: "512Mi"

