apiVersion: apps/v1
kind: Deployment

metadata:
  name: tic-tac-toe

spec:
  replicas: 3

  selector:
    matchLabels:
      app: tic-tac-toe

  template:
    metadata:
      labels:
        app: tic-tac-toe  

    spec:

      containers:
      - name: tic-tac-toe

        image: agdevopsacr2026.azurecr.io/tic-tac-toe:84a6b5c55f9bf88f6e0a251dd10def04c05a0d85

        imagePullPolicy: Always

        ports:
        - containerPort: 5001

        volumeMounts:
        - name: secrets-store-inline
          mountPath: "/mnt/secrets-store"
          readOnly: true

      volumes:
      - name: secrets-store-inline
        csi:
          driver: secrets-store.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: "azure-keyvault"
