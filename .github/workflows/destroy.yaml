- name: Terraform Destroy
  run: terraform destroy -auto-approve

- name: Destroy Completed
  working-directory: .
  run: |
    echo ""
    echo "========================================"
    echo "Platform destroyed successfully."
    echo "========================================"
    echo ""
    echo "If Key Vault enters soft-delete later, the next"
    echo "Platform Infrastructure run will recover it automatically."