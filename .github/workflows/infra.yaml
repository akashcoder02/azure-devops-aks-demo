name: Platform Infrastructure

on:
  workflow_dispatch:

env:
  RESOURCE_GROUP: rg-devops-demo
  AKS_NAME: aks-devops-demo
  KEYVAULT_NAME: agdevopskv2026

jobs:
  terraform:

    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: terraform

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Recover Key Vault (If Deleted)
        run: |
          if az keyvault list-deleted --query "[?name=='$KEYVAULT_NAME']" -o tsv | grep -q "$KEYVAULT_NAME"; then
            echo "Recovering deleted Key Vault..."
            az keyvault recover \
              --name $KEYVAULT_NAME
          else
            echo "No deleted Key Vault found. Continuing..."
          fi

      - name: Wait for Key Vault Recovery
        run: |
          until az keyvault show --name $KEYVAULT_NAME >/dev/null 2>&1; do
            echo "Waiting for Key Vault to become available..."
            sleep 5
          done

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init
        run: terraform init

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        run: terraform plan

      - name: Terraform Apply
        run: terraform apply -auto-approve

      - name: Platform Bootstrap
        run: echo "Starting Platform Bootstrap..."


      - name: Create Default Secret
        run: |
          az keyvault secret set \
            --vault-name $KEYVAULT_NAME \
            --name app-secret \
            --value "HelloFromAzureKeyVault" \
            --only-show-errors

      - name: Verify Secret
        run: |
          az keyvault secret show \
            --vault-name $KEYVAULT_NAME \
            --name app-secret \
            --query id

      - name: Show Outputs
        run: terraform output