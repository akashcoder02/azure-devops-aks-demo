name: Destroy Argo CD

on:
  workflow_dispatch:

jobs:

  destroy-argocd:

    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: terraform/addons/argocd

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Verify AKS Cluster
        run: |
          echo "Checking AKS Cluster..."

          az aks show \
            --resource-group ${{ secrets.RESOURCE_GROUP }} \
            --name ${{ secrets.AKS_CLUSTER }} \
            --output table

      - name: Get AKS Credentials
        run: |
          az aks get-credentials \
            --resource-group ${{ secrets.RESOURCE_GROUP }} \
            --name ${{ secrets.AKS_CLUSTER }} \
            --overwrite-existing

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init
        run: terraform init

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan Destroy
        run: terraform plan -destroy

      - name: Terraform Destroy
        run: terraform destroy -auto-approve

      - name: Verify Removal
        run: |
          kubectl get namespace argocd || true

      - name: Destroy Summary
        run: |
          echo ""
          echo "========================================"
          echo "ARGO CD DESTROY COMPLETED"
          echo "========================================"

          echo ""
          echo "Namespace:"
          kubectl get namespace argocd || echo "Namespace removed."

          echo ""
          echo "========================================"